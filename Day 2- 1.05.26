// Problem 1 -- If condition 
// to verify user's age wether eligible for vote or not ?
#include <iostream>
using namespace std;

int main() {
    int age;
    cin >> age;
    // if condition 
    if(age >= 18) {
        // logic 
        cout << "Eligible For vote";
    }
    return 0;
}

// Problem 2 -- If - else condition 
// to verify user's age wether eligible for vote or not ?
#include <iostream>
using namespace std;

int main() {
    int age;
    cin >> age;
    // if condition 
    if(age >= 18) {
        // logic 
        cout << "Eligible For vote";
    } else {
        // logic 
        cout << "Not Eligible For vote";
    }
    return 0;
}

//Problem-3 -- Nested If - else condition
//to verify users age wether wligible for vote or not?
#include<iostream>
using namespace std;

int main(){
    int age;
    cin >> age;
    
    if(age >= 18){
        if(age >= 100){
            cout << "Eligible for vote (But we are super senior citizen";
        }else{
            cout << "Eligible for vote";
        }
    }else{
        if(age <0){
            cout << "Enter a valid age";
        }else{
            cout << "Not Eligible for vote";
        }
    }
    return 0;
}


//Problem 4 -- Ternary Operator 
// Find the Greater number amoung 2 
#include<iostream>
using namespace std;

int main() {
    int a, b;
    int max_Number;
    
    cin >> a >>b;
    //Ternary Operator
    max_Number = (a > b) ? a : b;
    
    cout << max_Number;
    return 0;
}


//problem 5 -- contine statement
//print 1 to N and avoid those number which are divisible by Problem-3

#include<iostream>
using namespace std;

int main(){
    int n;
    cin >> n;
    
    for (int i =1; <=n; i++){
        if( i % 3 == 0){
            continue;
        }
        cout  << i << " ";

    }
    cout << "/n";
    retun 0;
    
}



//problem 6 -- Break statement
#include<iostream>

using namespace std;

int main(){
    int n;
    int sum = 0;
    
    while(true){
    cin >> n;
    
    if(num < 0){
        break;
        
    }
    sum = sum + n;
}
cout << sum;
return 0;
}



//Problem 7 ---Smart home lighting system


#include<iostream>
using namespace std;

int main() {
    string dayOrNight;
    string motionDetected;
    
    cout << "is it day or night? ; ";
    cin >> dayOrNight;
    cout << "Is Motion Detected? (1 for Yes, 0 for No): ";
    cin >> motionDetected;
    
    if(dayOrNight == "Night"  || dayOrNight == "night"){
        if(motionDetected == "Yes") { 
            cout << "Turn on the Led light with 100% brightness...";
        } else {
            cout << "Turn on the led light with only 10% brightness..";
            
        } 
        } else {
            cout << "turn off the Lights...";
        }
    return 0;




 int main(){
        int choice;
        cin >> choice;
        
        switch(choice)
{
    case 1:
    cout << "transferring call to customer service....";
    
    case 2:
    cout<<"transferring call to billing and accounts..";
    
    case 3:
    cout <<"connecting to the techinal help desk...";
    
    case 4:
    cout <<"connecting call to the agent.....";
    
    default:
    cout <<"Invalid selection enter a valid number,,,,";
    
    
    
}   return 0; 
             }

